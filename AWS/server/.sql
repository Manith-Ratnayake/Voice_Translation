CREATE database VoiceTranslationProgram;
USE VoiceTranslationProgram;


CREATE TABLE USERS (
	  ip varchar(15),
    username varchar(50),
    language_speak varchar(50),
    PRIMARY KEY (username)
);


INSERT INTO USERS()
VALUES(25501, "Manith", "English");
